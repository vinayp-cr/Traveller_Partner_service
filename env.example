# Database Configuration
# Option 1: Use full DATABASE_URL (highest priority)
# DATABASE_URL=mysql+pymysql://root:password@host:port/database

# Option 2: Use individual environment variables (overrides db.json)
# DB_HOST=your-db-host
# DB_USER=your-db-user
# DB_PASSWORD=your-db-password
# DB_NAME=your-db-name
# DB_PORT=3306
# DB_DRIVER=mysql+pymysql

# Option 3: Use JSON configuration (see app/config/db.json)
# Set DB_ENVIRONMENT to choose which config to use: default, development, production
# DB_ENVIRONMENT=default

# Xeni API Configuration
XENI_API_KEY=your_xeni_api_key_here

# API Configuration File (optional)
# Set to api_config_prod.json for production, api_config_dev.json for development
# Defaults to api_config.json if not set
API_CONFIG_FILE=api_config.json

# Application Configuration
PYTHONPATH=/app
